<form
  hx-post="{{ post_url }}"
  hx-target="this"
  hx-disabled-elt="find input[type='submit']"
  hx-swap="outerHTML"
>
  {% csrf_token %}
  {{ form.as_p() }}
  <input type="submit" value="{% trans %}Save{% endtrans %}">
</form>
